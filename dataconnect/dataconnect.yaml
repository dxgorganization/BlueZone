specVersion: "v1beta"
serviceId: "tournamentapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tournamentapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
